// Render pipeline construction.
//
// This module will build the wgpu RenderPipeline for the naive renderer
// using the vertex/fragment shaders in shaders/.
// Stubbed here so main.rs compiles — filled in during the geometry phase.